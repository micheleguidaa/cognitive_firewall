# Copy to .env (git-ignored) and fill in. Each CF_MAIN_* defaults to its CF_* counterpart.

# -- Oversight: gates + judge --
CF_BACKEND=     # openai | local
CF_MODEL=
CF_API_KEY=
CF_BASE_URL=    # endpoint for a 'local' backend

# -- Governed main model (set only what differs) --
CF_MAIN_BACKEND=
CF_MAIN_MODEL=
CF_MAIN_BASE_URL=

# -- Datasets --
HF_TOKEN=

# -- Advanced (optional; defaults: flag, false) --
# CF_FAIL_MODE=
# CF_DRY_RUN=
