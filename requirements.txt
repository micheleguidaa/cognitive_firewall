# Core + evaluation. Every gate runs on a real model, so `openai` (the
# OpenAI-compatible client) is required; the rest power the API/UI and eval harness.
#
# Freeze exact versions for the paper with:  pip freeze > requirements.lock.txt
openai>=1.40
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.36
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27
pytest>=8.0
datasets>=2.19
scikit-learn>=1.4
pandas>=2.2
matplotlib>=3.8
